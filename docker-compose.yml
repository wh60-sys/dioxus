services:
  rust:
    #image: rust:latest
    image : rust:1.94.0
    container_name: dioxus-rust
    working_dir: /app
    volumes:
      - .:/app
    ports:
      #- "3000:3000"
      - "8080:8080"
    stdin_open: true
    tty: true
    command: ["tail", "-f", "/dev/null"]
